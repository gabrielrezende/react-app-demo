import AppSystem from './Main/AppSystem';
import { BrowserRouter as Router } from 'react-router-dom';

export default () => {
    return (
        <Router>
            <AppSystem/>
        </Router>
    )
}